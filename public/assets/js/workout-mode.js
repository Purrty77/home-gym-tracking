document.addEventListener('DOMContentLoaded',()=>{
  const form=document.querySelector('#active-set-form');if(!form)return;
  const token=form.querySelector('[name="_token"]').value,weight=document.querySelector('#workout-weight'),reps=document.querySelector('#workout-reps'),overlay=document.querySelector('#workout-overlay'),restPanel=document.querySelector('#overlay-rest'),completePanel=document.querySelector('#overlay-complete'),countdown=document.querySelector('#rest-countdown'),error=document.querySelector('#set-error'),actionError=document.querySelector('#set-action-error');let timer,remaining=window.workoutMode.remaining||0,paused=window.workoutMode.paused||0;
  document.querySelectorAll('[data-workout-mode] button').forEach(button=>button.style.touchAction='manipulation');
  document.querySelectorAll('[data-adjust-weight]').forEach(button=>button.onclick=()=>weight.value=Math.max(0,(parseFloat(weight.value)||0)+parseFloat(button.dataset.adjustWeight)*window.workoutMode.increment).toFixed(2));
  document.querySelectorAll('[data-adjust-reps]').forEach(button=>button.onclick=()=>reps.value=Math.max(0,(parseInt(reps.value)||0)+parseInt(button.dataset.adjustReps)));
  document.querySelectorAll('[data-quick-reps]').forEach(button=>button.onclick=()=>reps.value=button.dataset.quickReps);
  const post=async(url,data={})=>{const response=await fetch(url,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({_token:token,...data})}),json=await response.json();if(!response.ok)throw new Error(json.error||'Unable to save.');return json;};
  const confirmRemoval=()=>new Promise(resolve=>{const modal=document.createElement('div');modal.className='fixed inset-0 z-[110] flex items-center justify-center bg-black/75 p-4 backdrop-blur-sm';modal.innerHTML='<div class="w-full max-w-md rounded-3xl border border-zinc-700 bg-zinc-900 p-6 shadow-2xl"><div class="flex h-12 w-12 items-center justify-center rounded-2xl bg-red-950 text-2xl font-black text-red-300">!</div><h2 class="mt-4 text-xl font-black">Remove completed set?</h2><p class="mt-2 text-zinc-400">This set has already been recorded. Removing it will update this workout permanently.</p><div class="mt-6 grid grid-cols-2 gap-3"><button data-no class="btn-secondary">Keep set</button><button data-yes class="btn-danger">Remove set</button></div></div>';document.body.append(modal);const close=value=>{modal.remove();resolve(value);};modal.querySelector('[data-no]').onclick=()=>close(false);modal.querySelector('[data-yes]').onclick=()=>close(true);modal.onclick=event=>{if(event.target===modal)close(false);};});
  const timerRequest=action=>post('/workouts/active/timer',{action});
  const render=()=>{countdown.textContent=remaining<=0?'READY':`${String(Math.floor(remaining/60)).padStart(2,'0')}:${String(remaining%60).padStart(2,'0')}`;if(remaining<=0){clearInterval(timer);countdown.classList.add('text-emerald-400');document.querySelector('[data-timer="skip"]').textContent='Start set';try{if(window.workoutMode.vibration)navigator.vibrate?.([180,80,180]);if(window.workoutMode.sound){const audio=new AudioContext(),oscillator=audio.createOscillator(),gain=audio.createGain();oscillator.connect(gain);gain.connect(audio.destination);gain.gain.value=.05;oscillator.frequency.value=660;oscillator.start();oscillator.stop(audio.currentTime+.16);}}catch{}}};
  const startTimer=seconds=>{remaining=seconds;overlay.classList.remove('hidden');overlay.classList.add('flex');restPanel.classList.remove('hidden');completePanel.classList.add('hidden');render();clearInterval(timer);timer=setInterval(()=>{remaining--;render();},1000);};
  const showComplete=(title,detail,icon='✅')=>{clearInterval(timer);overlay.classList.remove('hidden');overlay.classList.add('flex');restPanel.classList.add('hidden');completePanel.classList.remove('hidden');document.querySelector('#complete-icon').textContent=icon;document.querySelector('#complete-title').textContent=title;document.querySelector('#complete-detail').textContent=detail;};
  form.onsubmit=async event=>{event.preventDefault();error.classList.add('hidden');const button=document.querySelector('#complete-set');button.disabled=true;button.textContent='Saving…';try{const response=await fetch('/workouts/active/set',{method:'POST',body:new FormData(form)}),data=await response.json();if(!response.ok)throw new Error(data.error||'Unable to save the set.');if(data.status==='set_completed'){startTimer(data.rest_seconds);if(data.personal_record)restPanel.querySelector('p').textContent='🏆 PERSONAL RECORD · REST';}else if(data.status==='exercise_completed')showComplete(`${data.exercise} complete`,`${data.best_weight} kg × ${data.best_reps}. Next: ${data.next_exercise}`,data.personal_record?'🏆':'✅');else location.href='/workouts/active/summary';}catch(exception){error.textContent=exception.message;error.classList.remove('hidden');button.disabled=false;button.textContent='✅ Complete set';}};
  document.querySelector('#add-workout-set').onclick=async event=>{event.currentTarget.disabled=true;try{await post('/workouts/active/add-set');location.reload();}catch(exception){actionError.textContent=exception.message;actionError.classList.remove('hidden');event.currentTarget.disabled=false;}};
  document.querySelectorAll('[data-remove-extra-set]').forEach(button=>button.onclick=async()=>{const completed=button.dataset.completed==='1';if(completed&&!(await confirmRemoval()))return;button.disabled=true;try{await post('/workouts/active/remove-set',{set_id:button.dataset.removeExtraSet,confirmed:completed?'1':'0'});location.reload();}catch(exception){actionError.textContent=exception.message;actionError.classList.remove('hidden');button.disabled=false;}});
  const bodyWeightForm=document.querySelector('#workout-body-weight-form');bodyWeightForm.onsubmit=async event=>{event.preventDefault();const button=bodyWeightForm.querySelector('button'),status=document.querySelector('#body-weight-status');button.disabled=true;try{await post('/workouts/active/body-weight',{weight_kg:document.querySelector('#workout-body-weight').value});status.textContent='Saved';status.classList.remove('hidden');}catch(exception){status.textContent=exception.message;status.className='text-xs text-red-400';}finally{button.disabled=false;}};
  const demoModal=document.querySelector('#exercise-demo-modal'),demoMedia=document.querySelector('#exercise-demo-media'),demoSteps=document.querySelector('#exercise-demo-steps');const closeDemo=()=>{demoModal?.classList.add('hidden');demoModal?.classList.remove('flex');document.body.classList.remove('overflow-hidden');if(demoMedia)demoMedia.removeAttribute('src');};document.querySelector('#open-exercise-demo')?.addEventListener('click',()=>{const source=demoMedia.dataset.gif||demoMedia.dataset.image;if(source)demoMedia.src=source;demoMedia.onerror=()=>{if(demoMedia.dataset.image&&demoMedia.src!==demoMedia.dataset.image)demoMedia.src=demoMedia.dataset.image;else demoMedia.onerror=null;};demoSteps.replaceChildren();const steps=window.workoutDemo?.steps?.length?window.workoutDemo.steps:[window.workoutDemo?.instructions].filter(Boolean);steps.slice(0,5).forEach((step,index)=>{const row=document.createElement('p');row.className='flex gap-2';const number=document.createElement('strong');number.className='text-sky-400';number.textContent=`${index+1}.`;const text=document.createElement('span');text.textContent=step;row.append(number,text);demoSteps.append(row);});demoModal.classList.remove('hidden');demoModal.classList.add('flex');document.body.classList.add('overflow-hidden');});document.querySelector('#close-exercise-demo')?.addEventListener('click',closeDemo);demoModal?.addEventListener('click',event=>{if(event.target===demoModal)closeDemo();});
  document.querySelectorAll('[data-timer]').forEach(button=>button.onclick=async()=>{const action=button.dataset.timer;if(action==='skip'){await timerRequest('skip');location.reload();return;}if(action==='add'){const data=await timerRequest('add');remaining=data.remaining;render();return;}if(action==='pause'){if(timer){clearInterval(timer);timer=null;const data=await timerRequest('pause');paused=data.remaining||remaining;button.textContent='Resume';}else{const data=await timerRequest('resume');remaining=data.remaining;button.textContent='Pause';startTimer(remaining);}}});
  document.querySelector('#continue-workout').onclick=()=>location.reload();if(remaining>0)startTimer(remaining);else if(paused>0){remaining=paused;startTimer(remaining);clearInterval(timer);timer=null;document.querySelector('[data-timer="pause"]').textContent='Resume';}
});
